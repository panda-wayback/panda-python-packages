# panda-utils

这是一个基础工具库。

## 安装

```bash
pip install panda-utils --index-url https://x-access-token:${GITHUB_TOKEN}@npm.pkg.github.com/OWNER/REPOSITORY
```

## 使用

```python
from panda_utils import hello
print(hello())
```
